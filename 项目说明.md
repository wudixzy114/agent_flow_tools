核心理论。

现在AI Agent极其强大，我们可以使用很多强大的AI Agent开发很大型的项目。
但是有几个最主要的问题：

1. 信息熵理论：
   AI本质是对概率最大值的补充。比如，如果我说一句话，那么AI会根据训练数据等内容进行大规模补充，我称之为边缘信息。但很可惜，按照世界的规律，边缘信息是不具有核心价值的。也就是说，我需要把项目的核心价值告诉AI，才能产出有价值的产品。
   从这个角度看，提供的有效信息的质量和数量，决定了能够支持多大规模，多高质量的项目。

2. 实践难题理论：
   我希望开发一个3A游戏，但是众所周知，3A需要大量的代码，这很显然是AI无法一次生成的，即使它有这个能力。所以，它往往会开始降级实现，或者保守实现，这样做出来的永远是一个demo，后续迭代开发也极其困难。
   从这个角度看，我们需要在一开始就设计好总架构，以及非常全面的细节。这样才能让AI逐个攻克，并且能够实现并行开发。俗话说：先谋而后动，先胜后战。

3. 工程实践理论：
   大型项目永远不是只有开发，还有测试，审核，重构，性能检测，日志记录，用户反馈等等，我们需要搭建一个工作流，自由编排。
   比如，我们可以先用一个Agent进行需求对齐，然后用另一个Agent进行开发和测试，再让下一个Agent进行对抗审核，完成验收。如果涉及性能，还要有性能对比的环节。
   在性能对比的环境，AI无法直接对比，需要提前编写好工具脚本，提供给AI，让它调用即可。
   从这个角度看，我们需要编排好流程，并且进行调度，同时还要提前准备好项目相关的工具。

4. 工具迭代理论：
   俗话说：磨刀不误砍柴功。在开发的过程中，为了解决重复性问题等，需要不断开发工具，迭代工具，达到螺旋式上升的目的。

所以，这个项目就是为了达到上面4个目的：
我提出：

1. 递归任务分解：
   我们只需要一个项目说明文件。
   这个文件 -> AI分析并分解 -> AI提出大量的问题 -> 用户补充信息 -> AI最后确认 -> AI产生子任务。
   对于每个子任务，重复上述过程。
   直到子任务是可以一次完成的原子任务。

   我们需要什么：
   1. 这需要一个可视化图（节点图），让用户轻松查看检查。
   2. 这需要一个漂亮的html网页，让用户做选择题（多选）和填空题，快速补充信息。
   3. 每个子节点需要生成一份详细的任务单和向上提供的服务接口任务单，提供给AI Agent。
   4. 需要有一个对整个项目的说明，让每一个AI Agent都能知道最终目标。

   在分解完成后，我们可以并行调用AI Agent，逐一完成所有子任务，然后不断拼接，完成整个项目。

2. 工作流系统：
   对于每一个任务，我们应该如何实现？
   每一个任务 + 全局项目说明 + 这一步应该完成的任务 -> AI Agent。
   当然，我们还需要提供给AI工具使用说明。

   我们需要什么：
   1. 同样是一个可视化图。
   2. 每个节点可以接入工具说明（文档还是skill还是mcp？你可以看看哪种最好）

3. 接入主流Agent工具：
   我们的AI Agent有的是通过codex调用的，有的是通过copliot调用的，有的则是Opencode，这就意味着我们需要能够调用这些工具，并查看它们是否正常运行。这样我们才能监控每个子Agent的状态。
   这该如何实现？我不知道，反代可以吗？你可以想一个最佳方案。

4. 自监督迭代系统：
   我们让一个AI做监控，每一个子AI Agnet完成任务后，会触发报告系统（回调实现？），唤醒监控系统，然后它根据情况派发下一个任务。监控不只有一层，可能有很多层，这样的话，就可以零监督的情况下完成整个任务。

请帮我开始完成这个项目，以最高标准。
